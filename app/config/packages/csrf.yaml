# Symfony forms use the standard CSRF token id configured below.
# Login and logout use their own session-backed CSRF tokens in security.yaml.
framework:
    form:
        csrf_protection:
            token_id: submit
